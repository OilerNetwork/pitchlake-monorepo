
services:
  # Local database service (only used if pitchlake-db is not available)

  juno_plugin:
    platform: linux/amd64
    volumes:
      - type: bind
        source: $HOME/snapshots-test/
        target: /snapshots

    build:
      context: .
      dockerfile: Dockerfile.local
    environment:
      UDC_ADDRESS: ${UDC_ADDRESS}
      RPC_URL: ${RPC_URL}
      L1_URL: ${L1_URL}
      PITCHLAKE_DB_URL: postgres://pitchlake_user:pitchlake_password@pitchlake-db:5432/pitchlake?sslmode=disable
    networks:
      - pitchlake-network
    ports:
      - "6060:6060" # Adjust this port if needed

volumes:
  postgres_data:

networks:
  pitchlake-network:
    external: true
    name: pitchlake-network
