run:
  modules-download-mode: readonly
  timeout: 5m
  skip-dirs:
    - juno

version: "2"

linters:
  disable:
    - errcheck
    - ineffassign
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
